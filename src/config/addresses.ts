import { addresses as BaseAddresses } from "./base";
import { addresses as InkAddresses } from "./ink";
import { addresses as UnichainAddresses } from "./unichain";

export const addresses = {
  base: BaseAddresses,
  ink: InkAddresses,
  unichain: UnichainAddresses,
};
